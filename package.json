{
  "name": "kit-skills",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "lint-staged": "^11.0.0"
  }
}